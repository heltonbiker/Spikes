<Window x:Class="HelloDataflow.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HelloDataflow"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <DockPanel>
		<StackPanel Orientation="Vertical" HorizontalAlignment="Left" VerticalAlignment="Top">
			<Button Content="Adiciona X" Command="{Binding ComandoAdicionaX}"/>
			<Button Content="Adiciona Y" Command="{Binding ComandoAdicionaY}"/>
		</StackPanel>
		<ListView ItemsSource="{Binding Xs}"/>
		<ListView ItemsSource="{Binding Ys}"/>
		<ListView ItemsSource="{Binding Pontos}"/>
	</DockPanel>
</Window>
