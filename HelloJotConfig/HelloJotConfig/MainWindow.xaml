<Window x:Class="HelloJotConfig.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HelloJotConfig"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
		<UniformGrid Columns="1" HorizontalAlignment="Center" VerticalAlignment="Center" Width="300" Height="200">
			<TextBlock Text="{Binding Value1}"/>
			<Slider Minimum="0" Maximum="100" Value="{Binding Value1}"/>
			<TextBlock Text="{Binding Value2}"/>
			<Slider Minimum="0" Maximum="100" Value="{Binding Value2}"/>
		</UniformGrid>
	</Grid>
</Window>
